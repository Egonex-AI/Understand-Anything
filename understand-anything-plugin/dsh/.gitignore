# Generated by `node scripts/sync-skills.mjs` (run automatically by `prepack`).
# These mirror the parent plugin's skills/agents/core and the packed grammars;
# they are build/pack artifacts, not source.
skills/
agents/
packages/
vendor/
node_modules/
*.tgz
